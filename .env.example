# Optional: Alerts.in.ua API token (for fallback historical data)
# Not required for primary live mode (raid.fly.dev is token-free)
ALERTS_API_TOKEN=

# Data cache directory (default: ./data)
DATA_DIR=./data
